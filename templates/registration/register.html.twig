{% extends 'base.html.twig' %}
{% block title %}Register{% endblock %}
{% block body%}
  <section class="w-100 p-4 d-flex justify-content-center pb-4 border">
    <div style="width: 26rem;">
      <!-- Pills navs -->
      <ul class="nav nav-pills nav-justified mb-3" id="ex1" role="tablist">
        <li class="nav-item" role="presentation">
          <a class="nav-link active" id="tab-login" data-mdb-toggle="pill" href="#pills-login" role="tab" aria-controls="pills-login" aria-selected="true">Inscription</a>
        </li>
      </ul>
      <!-- Pills navs -->

      <!-- Pills content -->
      <div class="tab-content">
        <div class="tab-pane fade show active" id="pills-login" role="tabpanel" aria-labelledby="tab-login">
          {{ form_start(registrationForm) }}

          <div class="my-custom-class-for-errors">
            {{ form_errors(registrationForm) }}
          </div>

          

         <!-- Name input -->
          <div class="row justify-content-center">
            <div class="col-6 ml-md-auto">
              {{form_widget(registrationForm.roles)}}
            </div>
            {% do 
              registrationForm.roles.setRendered %}

          </div>


          <!-- Name input -->

          <!-- Name input -->
          <div class="form-outline mb-4">
            <input type="text" class="form-control" id="{{ registrationForm.nom.vars.full_name }}" name="{{ registrationForm.nom.vars.full_name }}" placeholder="Nom" autocomplete="Nom">
            <div class="my-custom-class-for-errors">
              {{ form_errors(registrationForm.nom) }}
            </div>
            {% do 
              registrationForm.nom.setRendered %}
            <label class="form-label" for="registerName">Nom</label>
          </div>

          <!-- Username input -->
          <div class="form-outline mb-4">
            <input type="text" name="{{ registrationForm.prenom.vars.full_name }}" id="{{ registrationForm.prenom.vars.full_name }}" class="form-control" placeholder="Prenom">
            <div class="my-custom-class-for-errors">
              {{ form_errors(registrationForm.prenom) }}
            </div>
            {% do 
              registrationForm.prenom.setRendered %}
            <label class="form-label" for="registerUsername">Prenom</label>
          </div>

          <div class="form-outline mb-4">
            <input type="text" name="{{ registrationForm.telephone.vars.full_name }}" id="{{ registrationForm.telephone.vars.full_name }}" class="form-control" placeholder="Phone"/>
            <div class="my-custom-class-for-errors">
              {{ form_errors(registrationForm.telephone) }}
            </div>
            {% do 
              registrationForm.telephone.setRendered %}
            <label class="form-label" for="registerPhone">Phone</label>
          </div>

          <!-- Email input -->
          <div class="form-outline mb-4">
            <input type="email" name="{{ registrationForm.email.vars.full_name }}" id="{{ registrationForm.email.vars.full_name }}" class="form-control" placeholder="email"/>
            <div class="my-custom-class-for-errors">
              {{ form_errors(registrationForm.email) }}
            </div>
            {% do 
              registrationForm.email.setRendered %}
            <label class="form-label" for="registerEmail">Email</label>
          </div>

          <!-- Password input -->
          <div class="mb-4">

            {{
      form_row(registrationForm.plainPassword, {
        'attr' :{'class': 'form-control'}, label:"password"
            })
    }}

          </div>

          <!-- Checkbox -->
          <div class="form-check d-flex justify-content-center mb-4">
            {{ form_row(registrationForm.agreeTerms,{label:"consent"} ) }}
            <label class="form-check-label" for="registerCheck">
              I have read and agree to the terms
            </label>
          </div>

          <!-- Submit button -->
          <button type="submit" class="btn text-white btn-block mb-3" style="background-color:#1A237E">Register</button>
          {{ form_end(registrationForm) }}
        </div>

      </div>
      <!-- Pills content -->
    </section>
  {% endblock %}
